<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Wizarding World Quote App</title>
        <link rel ="stylesheet" href="/styles.css">
    </head>

    <body>
        <h1 data-position ="header">You're a quote, Harry</h1>
 
        <section data-position ="add-quote">
            <h2>Add quote</h2>
        <form action="/quotes" method="POST">
            <input type="text" placeholder="name" name="name">
            <input type="text" placeholder="quote" name="quote">
            <button type="submit">SUBMIT</button>
        </form>
        </section>

        <section data-position ="update-quote">
        <div>
            <h2>Lord Voldemort Attacks!</h2>
            <p>Replace the first Harry Potter quote with a quote written by Lord Voldemort</p>
            <button id="update-button">Replace Harry's quote</button>
        </div>
        </section>

        <section data-position ="delete-quote">
                <div>
                <h2>The Order Defends!</h2>
                    <p>Delete one Lord Voldemort quote. This does nothing if there are no more Lord Voldemort       quotes</p>
                    <button id="delete-button">Delete Lord Voldemort's quote</button>
                </div>
                <div id="message"></div>
        </section>


        <section data-position="quotes">
        <h2>Quotes</h2>
        <ul class="quotes">
            <% for (var i = 0; i < quotes.length; i++) { %>
                <li class="quote">
                    <% if (quotes[i].name) { %>
                        <span><%= quotes[i].name + ":" %></span>
                        <span><%= quotes[i].quote %></span>
                    <% } %>
                </li>
            <% } %>
        </ul>
        </section>

        
        <script src="/main.js"></script>
    </body>
</html>
